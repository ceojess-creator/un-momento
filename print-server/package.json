{
  "name": "unmomento-print-server",
  "version": "1.0.0",
  "description": "Un Momento booth print server — M100 + PixCut S1",
  "main": "server.js",
  "scripts": {
    "start": "node -r dotenv/config server.js",
    "dev":   "node -r dotenv/config server.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}